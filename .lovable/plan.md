## Change

In `src/components/memoir/Chapter.tsx`:

1. Swap lucide import: `ArrowLeft` → `ArrowUp`.
2. On the nav wrapper: change `justify-end` → `justify-center` so the button is centered under the body text.
3. Inside the button: replace `<ArrowLeft />` with `<ArrowUp />` and change the label from `Back` to `Top`.

No other changes — same Playlist-style button, same spacing, same `href="#toc"`.

### Files
- `src/components/memoir/Chapter.tsx`
